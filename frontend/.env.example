# F33 — origin of the finly API.
# Empty (or unset) keeps the relative /api/v1 path, which the Vite dev
# proxy handles. In production set this to the deployed backend origin,
# with no trailing slash and no /api/v1 suffix.
#   VITE_API_URL=https://finly-api.onrender.com
VITE_API_URL=
