# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
.venv/
.env

# Node
node_modules/
dist/
.env.local

# IDE
.vscode/
.idea/
*.swp

# DB
*.db
*.sqlite3

# OS
.DS_Store
Thumbs.db

# F34 — coverage artefacts are build output, not source. They were tracked, so
# every CI checkout started with one machine's stale run in place.
backend/.coverage
backend/coverage.xml
# F36 — the frontend half of the same rule: `vitest --coverage` writes an
# html/lcov tree here, which is build output and was not ignored.
frontend/coverage/

# Playwright run output — F37
frontend/test-results/
