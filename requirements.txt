# F34 — ONE list, at the repo root: Vercel's Python builder reads it there and
# cannot parse an `-r other/file.txt` include.
#
# F43 — this is now the RUNTIME list only: what the deployed function needs to serve a
# request. Test and local-dev tools live in requirements-dev.txt (which includes this file),
# so the function no longer installs pytest and friends into every cold start.
fastapi==0.115.6
sqlalchemy==2.0.36
alembic==1.14.0
pydantic[email]==2.10.3
pydantic-settings==2.7.0
python-jose[cryptography]==3.3.0
cryptography==43.0.3  # F31 — pinned directly; previously only available transitively via python-jose
bcrypt==4.3.0
python-multipart==0.0.12
numpy==2.2.5
httpx==0.28.1
# F34 — apscheduler removed. Both in-process schedulers are Vercel Cron
# endpoints now; a background thread in a function that is frozen between
# requests never fires, and never says so.
reportlab==4.2.5     # F28 — year-end PDF report (pure Python; no native deps)
psycopg2-binary==2.9.10  # F33 — driver for Render managed Postgres
