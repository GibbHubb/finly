# F34 — ONE list, at the repo root: Vercel's Python builder reads it there and
# cannot parse an `-r other/file.txt` include.
fastapi==0.115.6
uvicorn[standard]==0.32.1
sqlalchemy==2.0.36
alembic==1.14.0
pydantic[email]==2.10.3
pydantic-settings==2.7.0
python-jose[cryptography]==3.3.0
cryptography==43.0.3  # F31 — pinned directly; previously only available transitively via python-jose
bcrypt==4.3.0
python-multipart==0.0.12
numpy==2.2.5
pytest==8.3.4
httpx==0.28.1
pytest-asyncio==0.24.0
pytest-cov==6.0.0
factory-boy==3.3.1
pytest-factoryboy==2.7.0
# F34 — apscheduler removed. Both in-process schedulers are Vercel Cron
# endpoints now; a background thread in a function that is frozen between
# requests never fires, and never says so.
reportlab==4.2.5     # F28 — year-end PDF report (pure Python; no native deps)
psycopg2-binary==2.9.10  # F33 — driver for Render managed Postgres

# F34 — three dependencies that were USED but never DECLARED. All three were
# installed here by accident (pulled in transitively, or left over from an old
# venv), so every local run passed while a clean CI checkout could not even
# COLLECT the test suite: `ERROR tests/test_trends.py`, pytest exit code 2.
#
# `pydantic-settings` is the alarming one — app/core/config.py imports it at
# startup, so the deployed app was relying on it arriving by luck.
python-dateutil==2.9.0.post0
pydantic-settings==2.14.2
factory-boy==3.3.1
