[pytest]
testpaths = tests
pythonpath = .
asyncio_mode = auto
addopts =
    --strict-markers
    --strict-config
    -ra
    --cov=app
    --cov-report=term-missing
    --cov-report=xml
filterwarnings =
    ignore::DeprecationWarning
